{
  "name": "clubhouse-agent-protocol",
  "private": true,
  "version": "0.1.0",
  "description": "Open agent-facing edge of The Clubhouse — x402 gateway, SDKs, and MCP server",
  "license": "MIT",
  "workspaces": ["gateway", "packages/*"],
  "scripts": {
    "dev": "npm --workspace gateway run dev",
    "build": "npm --workspace gateway run build",
    "typecheck": "tsc --noEmit -p gateway/tsconfig.json"
  }
}
